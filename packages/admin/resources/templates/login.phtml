<?php
/**
 * @var callable|object $v
 * @var $metaTitlePrefix
 * @var $metaTitle
 * @var $metaTitleSuffix
 * @var $appName
 * @var $loginUri
 */
?>
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
<!--    <meta name="csrf-token" content="--><?php //echo $v->e($csrfToken); ?><!--">-->
    <title><?php echo $v("$metaTitlePrefix$metaTitle$metaTitleSuffix") ?></title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
    <style>
    body, .container {
        min-height: 100vh;
    }

    body {
        font-family: 'Montserrat', sans-serif;
        margin: 0;
        background-color: #fafafa;
        color: #667;
    }

    .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
    }

    h1 {
        font-size: 4.2rem;
        font-weight: normal;
        text-transform: uppercase;
        position: relative;
        margin: 0;
    }

    form {
        display: grid;
        grid-template-columns: auto auto;
        gap: .5rem;
        align-items: center;
        text-align: left;
    }

    label {
        font-weight: bold;
    }

    input, button {
        font: inherit;
    }

    button {
        grid-column: 1 / 3;
    }

    a {
        margin-top: .5rem;
        color: inherit;
    }

    a:hover {
        text-decoration: underline;
    }

    a,
    a:visited {
        text-decoration: none;
    }

    .links {
        font-size: .6rem;
        display: flex;
    }

    .links a {
        text-transform: uppercase;
        padding: 10px;
    }

    @media (min-width: 576px) {
        h1 {
            font-size: 5rem;
        }

        p {
            font-size: 1rem;
            letter-spacing: .6rem;
            text-indent: .5rem;
        }

        .links {
            font-size: .8rem;
        }
    }
    </style>
</head>
<body>
<main class="container">
    <h1>Admin</h1>
    <form action="<?php echo $v($loginUri) ?>" method="post">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" placeholder="Judy Doe">
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" placeholder="Secret">
        <button>Login</button>
    </form>
    <nav class="links">
      <a href="/">Go back to the homepage</a>
    </nav>
</main>
</body>
</html>
